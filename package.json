{
  "name": "f2x-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "browser:chrome": "npx cypress run --browser=chrome --headed --spec cypress/e2e/rest-api.cy.js --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.27.0",
    "cypress": "^13.6.6",
    "mocha-allure-reporter": "^1.4.0"
  }
}
